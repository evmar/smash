syntax = "proto3";
package proto;

message RunRequest {
  int32 cell = 1;
  string cwd = 2;
  repeated string argv = 3;
}

message TermText {
  int32 row = 1;
  message Span {
    int32 attr = 1;
    string text = 2;
  }
  repeated Span spans = 2;
}

message TermState {
  // e.g. cursor position
}

message Output {
  int32 cell = 1;
  oneof output {
    string error = 2;
    TermText text = 3;
    int32 exit_code = 4;
  }
}

message ServerMsg {
  oneof msg {
    Output output = 1;
  }
}
